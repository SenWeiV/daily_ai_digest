# Daily AI Digest - local-only deployment hints
# Copy to .env.local for values that should never be committed.

DIGEST_DEPLOY_SSH_HOST=<your-server-host>
DIGEST_DEPLOY_SSH_USER=<your-ssh-user>
DIGEST_DEPLOY_SSH_PORT=22
VITE_API_BASE_URL=http://<your-server-host>:8000/api
