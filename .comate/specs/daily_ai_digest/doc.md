# Daily AI Digest - 每日AI情报摘要系统

## 需求概述

构建一个自动化AI Agent工具，每天定时从GitHub和YouTube检索AI/Agent领域最热门内容，通过Gemini大模型进行深度分析后，以邮件形式发送每日情报摘要。

---

## 一、系统架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端展示层 (React)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │  数据看板   │  │  历史记录   │  │   配置管理/手动触发     │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │ REST API
┌─────────────────────────────────────────────────────────────────┐
│                      后端服务层 (FastAPI)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ 调度服务    │  │ API接口     │  │   邮件服务              │  │
│  │ APScheduler │  │ /api/*      │  │   Gmail SMTP            │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                       AI Agent 核心层                            │
│  ┌──────────────────┐  ┌──────────────────────────────────────┐ │
│  │ GitHub Agent     │  │ YouTube Agent                        │ │
│  │ - 趋势检索       │  │ - 视频检索                           │ │
│  │ - 仓库内容爬取   │  │ - 字幕/转录获取                      │ │
│  │ - 代码文件分析   │  │ - 视频内容理解                       │ │
│  └──────────────────┘  └──────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────────────────────┐│
│  │               Gemini 分析引擎                                ││
│  │  - 内容深度阅读与理解                                        ││
│  │  - 热度原因分析                                              ││
│  │  - 价值提炼与总结                                            ││
│  └──────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────────┐
│                        数据存储层                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  SQLite (digest_history, config, logs)                      ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈明细

| 层级 | 技术选型 | 版本 | 用途 |
|------|----------|------|------|
| 前端 | React | 18.x | 用户界面 |
| 前端 | TypeScript | 5.x | 类型安全 |
| 前端 | Tailwind CSS | 3.x | 样式框架 |
| 前端 | Vite | 5.x | 构建工具 |
| 后端 | Python | 3.11+ | 运行环境 |
| 后端 | FastAPI | 0.109+ | Web框架 |
| 后端 | APScheduler | 3.10+ | 定时任务 |
| 后端 | Uvicorn | 0.27+ | ASGI服务器 |
| AI | google-generativeai | latest | Gemini API |
| 数据采集 | PyGithub | 2.x | GitHub API |
| 数据采集 | google-api-python-client | latest | YouTube API |
| 数据采集 | youtube-transcript-api | latest | 获取字幕 |
| 数据采集 | Playwright | latest | 浏览器自动化(备用) |
| 存储 | SQLite | 内置 | 数据持久化 |
| 邮件 | smtplib | 内置 | Gmail发送 |

---

## 二、核心功能模块

### 2.1 GitHub Agent 模块

**功能**：检索前一天AI/Agent领域最热门的Top 10仓库，深度阅读项目内容。

**检索策略**：
```python
# 搜索关键词组合
keywords = [
    "AI agent", "LLM agent", "autonomous agent",
    "artificial intelligence", "machine learning",
    "large language model", "GPT", "transformer",
    "RAG", "vector database", "embedding"
]

# 排序维度
sort_options = ["stars", "forks", "updated"]

# 时间范围
date_range = "pushed:>{yesterday}"
```

**深度分析内容**：
- README.md 完整阅读
- 核心源码文件分析（自动识别入口文件）
- 项目结构理解
- 技术栈识别
- Star/Fork增长趋势

**输出结构**：
```python
class GitHubDigestItem:
    repo_name: str           # 仓库名
    repo_url: str            # 链接
    stars: int               # Star数
    stars_today: int         # 今日新增Star
    description: str         # 项目描述
    main_language: str       # 主要语言
    topics: List[str]        # 标签
    # Gemini分析结果
    summary: str             # 项目核心总结
    why_trending: str        # 为什么火
    key_innovations: List[str]  # 关键创新点
    practical_value: str     # 对用户的实用价值
    learning_points: List[str]  # 可学习的要点
```

### 2.2 YouTube Agent 模块

**功能**：检索前一天AI/Agent领域最热门的Top 10视频，深度理解视频内容。

**检索策略**：
```python
# 搜索关键词
keywords = [
    "AI agent tutorial", "LLM agent",
    "artificial intelligence news",
    "ChatGPT", "Claude AI", "Gemini AI",
    "machine learning 2024", "AI breakthrough"
]

# 过滤条件
filters = {
    "published_after": yesterday,
    "order": "viewCount",  # 按观看量排序
    "video_duration": "medium",  # 中等时长(4-20分钟)
    "relevance_language": "en"  # 优先英文
}
```

**深度分析流程**：
1. 获取视频元信息（标题、描述、观看量、点赞数）
2. 获取完整字幕/转录文本（优先官方字幕，其次自动生成）
3. 若无字幕，使用Playwright截取关键帧 + Gemini Vision分析
4. 将完整转录内容送入Gemini进行深度理解

**输出结构**：
```python
class YouTubeDigestItem:
    video_id: str            # 视频ID
    title: str               # 标题
    channel: str             # 频道名
    channel_url: str         # 频道链接
    video_url: str           # 视频链接
    view_count: int          # 观看量
    like_count: int          # 点赞数
    published_at: datetime   # 发布时间
    duration: str            # 时长
    # Gemini分析结果
    content_summary: str     # 内容完整总结
    key_points: List[str]    # 核心观点列表
    why_popular: str         # 为什么受欢迎
    practical_takeaways: str # 实用收获
    recommended_for: str     # 推荐人群
```

### 2.3 Gemini 分析引擎

**模型配置**：
```python
model_config = {
    "model": "gemini-1.5-pro",  # 或最新的gemini-2.0
    "temperature": 0.3,         # 较低温度保证准确性
    "max_output_tokens": 8192,
    "top_p": 0.8
}
```

**分析Prompt模板**：

GitHub项目分析：
```
你是一位资深AI技术专家和技术博主。请深度分析以下GitHub项目：

项目名称：{repo_name}
项目描述：{description}
主要语言：{language}
Star数：{stars}

README内容：
{readme_content}

核心代码文件：
{code_files}

请从以下维度进行分析：
1. 项目核心功能总结（100字内）
2. 为什么这个项目会火？（技术创新/解决痛点/时机把握）
3. 关键技术创新点（列出3-5点）
4. 对AI从业者/学习者的实用价值
5. 值得学习的技术要点
```

YouTube视频分析：
```
你是一位资深AI领域内容分析师。请深度分析以下视频内容：

视频标题：{title}
频道：{channel}
观看量：{view_count}
时长：{duration}

完整字幕/转录：
{transcript}

请从以下维度进行分析：
1. 视频核心内容总结（150字内）
2. 关键观点提炼（列出5-7个要点）
3. 为什么这个视频受欢迎？
4. 观看后的实用收获
5. 适合什么样的观众
```

### 2.4 定时任务调度

**调度配置**：
```python
scheduler_config = {
    "trigger": "cron",
    "hour": 8,
    "minute": 0,
    "timezone": "Asia/Shanghai",
    "misfire_grace_time": 3600  # 1小时容错
}
```

**执行流程**：
```
08:00 触发任务
  │
  ├─→ 并行执行 GitHub Agent (预计10-15分钟)
  │     ├─ 搜索热门仓库
  │     ├─ 获取仓库详情和文件
  │     └─ Gemini分析每个项目
  │
  ├─→ 并行执行 YouTube Agent (预计10-15分钟)
  │     ├─ 搜索热门视频
  │     ├─ 获取字幕/转录
  │     └─ Gemini分析每个视频
  │
  └─→ 汇总结果
        ├─ 生成邮件内容
        ├─ 发送邮件
        ├─ 保存到数据库
        └─ 更新前端数据
```

### 2.5 邮件服务

**邮件模板设计**：
```html
Subject: 🤖 Daily AI Digest - {date} | GitHub Top 10 + YouTube Top 10

正文结构：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 今日概览
- GitHub热门项目：10个
- YouTube热门视频：10个
- 数据更新时间：{timestamp}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🐙 GitHub Top 10
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. {repo_name} ⭐{stars} (+{today})
   {summary}
   🔥 为什么火：{why_trending}
   💡 实用价值：{practical_value}
   🔗 {url}
...

📺 YouTube Top 10
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. {title} ??{views}
   📺 {channel}
   {summary}
   🎯 核心观点：{key_points}
   🔗 {url}
...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
由 Daily AI Digest 自动生成
```

**Gmail SMTP配置**：
```python
email_config = {
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "use_tls": True,
    "sender_email": "{user_email}",
    "sender_password": "{app_specific_password}",  # 应用专用密码
    "recipient_email": "{user_email}"
}
```

---

## 三、前端页面设计

### 3.1 页面结构

```
/                    # 首页 - 数据看板
/history             # 历史记录
/settings            # 配置页面
```

### 3.2 首页数据看板

**布局**：
```
┌─────────────────────────────────────────────────────────────────┐
│  Daily AI Digest                    [手动触发] [设置]           │
├─────────────────────────────────────────────────────────────────┤
│  📊 今日统计                         上次更新: 2024-xx-xx 08:15  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ GitHub      │ │ YouTube     │ │ 邮件状态    │               │
│  │ 10 项目     │ │ 10 视频     │ │ ✅ 已发送   │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  🐙 GitHub Top 10              📺 YouTube Top 10                │
│  ┌─────────────────────────┐   ┌─────────────────────────────┐ │
│  │ 1. repo-name ⭐1234     │   │ 1. Video Title ??50K        │ │
│  │    简短描述...          │   │    Channel Name              │ │
│  │    [查看详情]           │   │    [查看详情]                │ │
│  ├─────────────────────────┤   ├─────────────────────────────┤ │
│  │ 2. ...                  │   │ 2. ...                      │ │
│  └─────────────────────────┘   └─────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 3.3 配置页面

**配置项**：
- API密钥配置（GitHub Token, YouTube API Key, Gemini API Key）
- 邮箱配置（发件邮箱、应用专用密码、收件邮箱）
- 定时任务配置（执行时间、时区）
- 搜索关键词自定义
- 手动触发执行

---

## 四、数据库设计

### 4.1 表结构

```sql
-- 每日摘要记录
CREATE TABLE digest_records (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    digest_date DATE UNIQUE NOT NULL,
    github_data JSON,          -- GitHub Top10 完整数据
    youtube_data JSON,         -- YouTube Top10 完整数据
    email_sent BOOLEAN DEFAULT FALSE,
    email_sent_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 配置表
CREATE TABLE config (
    key TEXT PRIMARY KEY,
    value TEXT,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 执行日志
CREATE TABLE execution_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    execution_time TIMESTAMP,
    status TEXT,               -- success/failed
    github_count INTEGER,
    youtube_count INTEGER,
    error_message TEXT,
    duration_seconds INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 五、项目文件结构

```
daily_ai_digest/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py              # FastAPI入口
│   │   ├── config.py            # 配置管理
│   │   ├── database.py          # 数据库连接
│   │   ├── models.py            # 数据模型
│   │   ├── schemas.py           # Pydantic模型
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── routes.py        # API路由
│   │   │   └── deps.py          # 依赖注入
│   │   ├── agents/
│   │   │   ├── __init__.py
│   │   │   ├── github_agent.py  # GitHub Agent
│   │   │   ├── youtube_agent.py # YouTube Agent
│   │   │   └── gemini_analyzer.py # Gemini分析引擎
│   │   ├── services/
│   │   │   ├── __init__.py
│   │   │   ├── scheduler.py     # 定时任务
│   │   │   ├── email_service.py # 邮件服务
│   │   │   └── digest_service.py # 摘要生成服务
│   │   └── utils/
│   │       ├── __init__.py
│   │       └── helpers.py       # 工具函数
│   ├── requirements.txt
│   ├── .env.example             # 环境变量模板
│   └── run.py                   # 启动脚本
│
├── frontend/
│   ├── src/
│   │   ├── components/          # 组件
│   │   │   ├── Dashboard.tsx
│   │   │   ├── GitHubList.tsx
│   │   │   ├── YouTubeList.tsx
│   │   │   ├── DetailModal.tsx
│   │   │   └── Settings.tsx
│   │   ├── pages/               # 页面
│   │   │   ├── Home.tsx
│   │   │   ├── History.tsx
│   │   │   └── Settings.tsx
│   │   ├── services/            # API服务
│   │   │   └── api.ts
│   │   ├── types/               # TypeScript类型
│   │   │   └── index.ts
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.js
│   └── vite.config.ts
│
├── data/
│   └── digest.db                # SQLite数据库
│
├── README.md
└── docker-compose.yml           # 可选：Docker部署
```

---

## 六、API接口设计

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/digest/today | 获取今日摘要 |
| GET | /api/digest/history | 获取历史摘要列表 |
| GET | /api/digest/{date} | 获取指定日期摘要 |
| POST | /api/digest/trigger | 手动触发生成 |
| GET | /api/config | 获取配置 |
| PUT | /api/config | 更新配置 |
| GET | /api/status | 获取系统状态 |
| GET | /api/logs | 获取执行日志 |

---

## 七、密钥配置指南（实施阶段提供详细步骤）

### 7.1 GitHub Personal Access Token
- 访问 GitHub Settings → Developer settings → Personal access tokens
- 生成Token，勾选 `public_repo` 权限

### 7.2 YouTube Data API Key
- 访问 Google Cloud Console
- 创建项目 → 启用 YouTube Data API v3
- 创建凭据 → API密钥

### 7.3 Gemini API Key
- 访问 Google AI Studio (aistudio.google.com)
- 获取 API Key

### 7.4 Gmail 应用专用密码
- 开启Google账号两步验证
- 访问 Google账号 → 安全性 → 应用专用密码
- 生成专用密码用于SMTP

---

## 八、预期成果

1. **自动化情报收集**：每天8点自动获取AI领域最新热点
2. **深度内容分析**：不是简单的标题聚合，而是真正阅读理解后的价值提炼
3. **邮件推送**：精美格式的邮件，随时随地获取情报
4. **可视化看板**：前端页面查看历史数据和详细分析
5. **可配置性**：支持自定义关键词、时间、邮箱等

---

## 九、扩展能力（后续迭代）

- 支持更多数据源（Twitter/X、Hacker News、arXiv）
- 支持多语言内容
- 支持RSS订阅输出
- 支持Webhook通知
- 支持部署到云平台